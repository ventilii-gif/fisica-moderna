name: Deploy su GitHub Pages

# Esecuzione solo manuale: se la sorgente di Pages non è ancora impostata su
# "GitHub Actions", un workflow automatico fallirebbe a ogni push senza motivo.
# Attiva Pages nelle impostazioni, poi avvia questo workflow dalla scheda Actions.
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Un solo deploy alla volta, senza annullare quello in corso.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configura Pages
        uses: actions/configure-pages@v5

      # Il sito è già statico: index.html sta nella radice, nessuna build.
      - name: Carica il contenuto del repository
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
